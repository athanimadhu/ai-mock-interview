rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{sessionId} {
      allow read, write: if true;  // For now, allow all access. In production, you should restrict this
    }
  }
}